// problem: How to empty an array in javascript?
//do not reset it to a new array, and do not loop through to pop each value.

let array = [3, 5, 6, 77, 45, 3, 2];

array.length = 0;

console.log(array);
